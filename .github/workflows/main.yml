name: ci pipeline
on: [push]

jobs:
    test:
    runs-on: ubuntu-latest
     steps:
       - uses: actions/checkout
         run: npm install
         run: npm run test
         run: npm run lint
    
